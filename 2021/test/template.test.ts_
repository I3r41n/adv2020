import { part1, part2 } from "../src/day"

const day = 
const testSource = `../test/data/day${day}`

describe('', () => {
    it('return ', async () => {
        const file = `${testSource}/test1`
                
        expect(await part1(file)).toBe(0)
    })
})

describe('', () => {
    it('return ', async () => {
        const file = `${testSource}/test1`
                
        expect(await part2(file)).toBe(0)
    })
})